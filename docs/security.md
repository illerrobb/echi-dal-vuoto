# Sicurezza

Minimo privilegio: read-only predefinito; nessuna cancellazione, shell distruttiva, scrittura di manoscritto/canone/decisioni o commit su main. Web è opzionale e soggetto a consenso. Nessuna chiave, API LLM o SDK. Dipendenze npm sono bloccate da lockfile e installate con script disabilitabili. Operare su branch `agent/*`, `proposal/*` o `chapter/*`; verificare diff e validazioni. Le decisioni immutabili richiedono supersessione esplicita e approvata.
