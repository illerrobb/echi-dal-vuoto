# Schemi

JSON Schema 2020-12 convalidati da AJV. YAML rimane il formato autore; il campo `schema` seleziona lo schema. I controlli trasversali (gate, riferimenti, catena dei beat) sono in `scripts/studio.mjs`.
